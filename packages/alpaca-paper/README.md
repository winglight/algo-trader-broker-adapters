# Alpaca Paper adapter (reserved)

This directory reserves the package boundary for a future `alpaca_paper` adapter.
It intentionally has no executable package, entry point, dependencies, or order
submission code in Phase 3. Development requires the next phase's reviewed and
approved implementation design.
